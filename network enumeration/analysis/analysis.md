# Findings: Network Enumeration Lab

During the lab, Kali Linux was used to perform reconnaissance on an Ubuntu VM.  
The objective was to identify active hosts, open ports, running services, and the operating system to assess potential attack surfaces.  

The scan revealed three main services: SSH (22), FTP (21), and HTTP (80). Each service provides a potential point of entry if not properly secured.
